import { View, Text, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {},
});

export default function DetailsInfoCardScreen() {
    return (
        <View style={styles.container}>
            <Text style={styles.text}>Details Info Card Screen</Text>
        </View>
    );
}
